/**
 * Placeholder entrypoint. The two-speed polling architecture (5-minute
 * active-set poll, 60-minute reconciliation sweep, nightly integrity check —
 * plans/04-Architecture-Sketch.md Phase 16) is deferred to a later pass.
 *
 * This file exists so the workspace resolves and the repo shape is right.
 */
export {};
