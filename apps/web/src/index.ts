/**
 * Placeholder entrypoint. This pass builds the foundation only — the pure
 * SLA/OLA engine (packages/core) and its database schema (packages/db).
 * The Next.js App Router dashboard, auth, and live provider wiring described
 * in plans/04-Architecture-Sketch.md Phase 17 are deferred to a later pass.
 *
 * This file exists so the workspace resolves and the repo shape is right.
 */
export {};
