The hero runway callout renders correctly — pulsing dot, live countdown ticking (00:35:05), runway/kind caption, active leg + linked issue line, positioned above the "Open in Zendesk" button. Let's revert the debug injection now.

Now let's rebuild `CaseJourney.tsx` into the flagship "Segmented Case Journey" treatment the audit calls out as the biggest visual gap, keeping all the existing math intact.

Now let's replace the render body. First, add a computed "dominant leg" + total minutes memo, and the first-handoff timestamp:

Good, takes ISO string — my `new Date(firstHandoffAt).toISOString()` call works correctly. Let's typecheck.

Clean. Let's verify visually.

This is an excellent match to the mockup's "Segmented Case Journey" flagship treatment — legend, inline-labeled segments, axis captions, 4-metric grid, and the attribution finding callout. Let's run the case-detail test suite to make sure nothing broke.

All passing. Now let's continue with the next Case Detail item: adding a progress bar + headroom phrasing to `CommitmentCard.tsx`, and per-node type-pill chips to `ActivityTimeline.tsx`.

Now add the progress bar + headroom line in the JSX, right after the counter/deadline paragraphs:

Now let's add per-node type-pill chips to `ActivityTimeline.tsx` (audit: mainly needs a per-node type-pill chip added, matching Stitch's "CLOCK START"/"SLA MET"/"HANDOFF" labels).